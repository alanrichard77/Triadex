services:
  - type: web
    name: triadex-mod1
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app -w 2 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:${PORT:-8000}
    autoDeploy: true
    healthCheckPath: /health

